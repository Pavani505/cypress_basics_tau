const {mochaGrep} = require('@applitools/test-utils')

const group = process.env.MOCHA_GROUP

module.exports = {
  timeout: 0,
  require: ['@swc-node/register'],
  reporter: 'mocha-multi',
  reporterOptions: [`spec=-,xunit=./logs/report${group ? `-${group}` : ''}.xml`],
  grep: mochaGrep(),
}

import type { OpenSettings, Eyes } from './types';
import { type Logger } from '@applitools/logger';
import { type CoreRequests } from './server/requests';
import { type Heartbeat } from './heartbeat';
type Options = {
    requests: CoreRequests;
    agentId: string;
    concurrency?: number;
    cwd?: string;
    heartbeat: Heartbeat;
    logger: Logger;
};
export declare function makeOpenEyes({ requests, agentId: defaultAgentId, concurrency, cwd, heartbeat, logger: mainLogger, }: Options): ({ settings, logger, }: {
    settings: OpenSettings;
    logger?: Logger | undefined;
}) => Promise<Eyes>;
export {};

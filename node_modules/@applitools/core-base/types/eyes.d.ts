import type { Eyes, VisualTest } from './types';
import { type Logger } from '@applitools/logger';
type Options = {
    test: VisualTest;
    logger?: Logger;
};
export declare function makeEyes({ test, logger: defaultLogger }: Options): Eyes;
export {};

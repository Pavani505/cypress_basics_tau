import { type Logger } from '@applitools/logger';
import { type Proxy } from '@applitools/req';
export type Upload = (options: {
    name: string;
    resource: Uint8Array | URL | string;
    gzip?: boolean;
    logger?: Logger;
}) => Promise<string>;
export declare function makeUpload({ settings, logger: defaultLogger, }: {
    settings: {
        uploadUrl: string;
        proxy?: Proxy;
    };
    logger?: Logger;
}): Upload;
